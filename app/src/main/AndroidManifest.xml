<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="choirhelper.silihasah.org">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.songlist.SongActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.singing.SingBassActivity"
            android:label="SING BASS"
            android:parentActivityName=".ui.songlist.SongActivity" />
        <activity android:name=".ui.practice.PickVoiceTypeActivity" />
        <activity
            android:name=".ui.singing.SingSopranActivity"
            android:parentActivityName=".ui.songlist.SongActivity" />
        <activity android:name=".ui.singing.singpractice.ListennSingActivity"></activity>
    </application>

</manifest>